{
  "name": "michizure-firestore-rules-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20 <25"
  },
  "scripts": {
    "test": "firebase emulators:exec --project demo-michizure --config ../../firebase.json --only firestore \"vitest run --reporter=verbose\"",
    "test:unit": "vitest run --reporter=verbose"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.16.0",
    "firebase-tools": "15.24.0",
    "vitest": "4.1.10"
  }
}
